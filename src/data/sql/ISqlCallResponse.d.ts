import ISqlResponse from 'artisan-core-data/lib/sql/ISqlResponse';

/**
 * ISqlCallResponse is a specific type of ISqlResponse that contains the result
 * of a SqlCall.
 */
interface ISqlCallResponse extends ISqlResponse {
}

export default ISqlCallResponse;
